__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/
.venv
/api_keys.env
multirun/
artifacts/
.cache/
archive/

# Run artifacts -------------------------------------------------------------
# outputs/ is ~20 GB, almost entirely LoRA weights, and fully regenerable from
# the code plus the prompt library. The evidence needed to check the findings is
# assembled into results/ by scripts/collect_political_results.py and IS tracked.
outputs/
artifacts/
*.safetensors
*.bin
*.pt
